---
// Product pillar, /platform/workforce. Thin glue over the shared PillarPage template
// so the pillars cannot drift apart. Content lives in src/content/platform/workforce.mdx.
import { getEntry } from 'astro:content';
import PillarPage from '../../components/platform/PillarPage.astro';

const entry = await getEntry('platform', 'workforce');
if (!entry) throw new Error('Missing content entry: src/content/platform/workforce.mdx');
---

<PillarPage entry={entry} />

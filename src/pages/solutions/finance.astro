---
import { getEntry } from 'astro:content';
import PillarPage from '../../components/platform/PillarPage.astro';
const entry = await getEntry('platform', 'finance');
if (!entry) throw new Error('Missing content entry: src/content/platform/finance.mdx');
---
<PillarPage entry={entry} />

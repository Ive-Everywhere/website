---
// Footer/legal page — served from the canonical eluu.ai page for now so the
// content stays authoritative (important for legal). Replace with a native v3
// page when one is built. 302 = temporary, so it flips cleanly when v3 owns it.
return Astro.redirect('https://eluu.ai/compare/zapier', 302);
---

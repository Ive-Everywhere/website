---
# Transcribed verbatim from
# `content-src/eluu-website-content/finalized-pages/workspace.md` (DRAFT).
# Value-driver tags and italic internal notes are stripped; `[VERIFY]` becomes
# `needsVerify: true`.
title: Workspace
description: Run as many agents as the work needs, all at once, and everything they produce stays in one place you can see.
publishedAt: 2026-08-03
order: 2

hero:
  eyebrow: Workspace
  headline: One workspace for your whole team of agents.
  subhead: Run as many as the work needs, all at once, and everything they produce stays in one place you can see.

pitch:
  heading: Run a team of agents, not one at a time.
  body: Hand the work to as many agents as it takes, and they run together in parallel. They share a skills library, each keeps its own memory, and every file, output, and dashboard lands in one place. Some agents belong to the team, some are yours.
  linkLabel: Explore the docs
  linkHref: /docs
  # Source visual note: "workspace view (agents running in parallel, files and
  # dashboards)".
  art: c2-b-plan

stats:
  # `[VERIFY a real ceiling]`.
  - value: In parallel
    label: run as many agents at once as the work needs
    needsVerify: true
  - value: No limit
    label: on the context each agent keeps
  - value: 24/7
    label: scheduled jobs run on their own

features:
  - headline: Run many at once
    eyebrow: A whole team of agents, working in parallel.
    body: Hand the work to as many agents as it takes. They run at the same time, so ten tasks don't sit in a queue behind each other.
    icon: ri-stack-line
  - headline: Infinite context, built in
    eyebrow: Every agent keeps everything it learns.
    body: The workspace stores it all with no limit. An agent keeps what it learned months ago instead of hitting a context ceiling and dropping it.
    icon: ri-infinity-line
  - headline: Shared skills
    eyebrow: Build a skill once, every agent can use it.
    body: A team skill library, so a workflow that works spreads across your agents instead of getting rebuilt each time.
    icon: ri-magic-line
  - headline: One place for the work
    eyebrow: Files, outputs, and dashboards in one home.
    body: Every file, report, and dashboard an agent makes is saved and searchable in the workspace, not buried in a chat log.
    icon: ri-folder-3-line
  - headline: Agents build live apps
    eyebrow: Dashboards, trackers, and boards that stay current.
    body: Agents build working apps that connect to your tools. Scheduled jobs keep them updated as the work happens, so the tracker is right when you open it.
    icon: ri-dashboard-line

# `[VERIFY: one anonymized line, role-attributed.]` — placeholder transcribed,
# never invented.
testimonial:
  quote: 'VERIFY: one anonymized line, role-attributed.'
  attribution: role-attributed, anonymized
  needsVerify: true

technical:
  - title: How parallel execution works
    body: running many agents at once
    href: /docs
    linkLabel: Docs
    icon: ri-stack-line
  - title: How agent memory is stored
    body: the infinite-context store
    href: /docs
    linkLabel: Docs
    icon: ri-database-2-line
  - title: How to build an app on Eluu
    body: apps with data, storage, and connections
    href: /docs
    linkLabel: Docs
    icon: ri-dashboard-line

finalCta:
  headline: Give your team a workspace of agents.
---

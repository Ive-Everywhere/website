---
# Transcribed verbatim from
# `content-src/eluu-website-content/finalized-pages/security.md` (DRAFT v2).
# Value-driver tags and italic internal notes are stripped; `[VERIFY]` becomes
# `needsVerify: true`.
#
# Note: the source calls section 3 "THREE STATEMENTS" rather than "THREE
# NUMBERS", because security has no natural metrics. It is the same slot and
# uses the same `stats` field.
title: Security
description: Decide what each agent can reach and do, and keep a record of every action it takes.
publishedAt: 2026-08-03
order: 4

hero:
  eyebrow: Security
  headline: Enterprise controls for every agent you run.
  subhead: Decide what each agent can reach and do, and keep a record of every action it takes.

pitch:
  heading: Your keys, your data, your rules.
  body: An agent that can reach your tools needs limits, the same way a new hire does. Bring your own model key or self-host so your data stays in your account, and give each agent only the tools and actions you approve.
  linkLabel: Explore the docs
  linkHref: /docs
  # Source visual note: "access / audit view (agent permissions, action log)".
  art: c3-b-audit

stats:
  - value: BYOK or self-host
    label: your data stays in your account
  - value: Per-agent control
    label: the tools and actions you approve, nothing more
  # `[VERIFY: add SOC 2 status here once confirmed]`.
  - value: Every action logged
    label: a full audit trail
    needsVerify: true

features:
  - headline: Control what reaches the model
    eyebrow: Decide what an agent can send to an LLM.
    body: Bring your own model key, or self-host, so your data flows through your account and not ours.
    icon: ri-key-2-line
  - headline: Restrict each agent
    eyebrow: Give each agent only the tools it needs.
    body: Limit the tools and data an agent can reach. Routine actions run on their own; risky ones wait for a person to approve.
    icon: ri-shield-keyhole-line
  - headline: Audit every action
    eyebrow: A full record of what every agent did.
    body: What it accessed, what it changed, and when. The trail is there when finance, legal, or a customer asks.
    icon: ri-file-list-3-line
  - headline: See and control cost
    eyebrow: Know what every agent and team is spending.
    body: See spend per agent and per team, and set a hard cap that stops an agent when it hits the limit.
    icon: ri-money-dollar-circle-line
  - headline: Works with any harness
    eyebrow: Your coding harnesses, under the same controls.
    body: Claude Code, Codex, and Cline connect through the same permission and audit layer, so agents built on them follow the same rules as everything else.
    icon: ri-terminal-box-line

# `[VERIFY: one anonymized line, role-attributed.]` — placeholder transcribed,
# never invented.
testimonial:
  quote: 'VERIFY: one anonymized line, role-attributed.'
  attribution: role-attributed, anonymized
  needsVerify: true

technical:
  - title: BYOK and self-hosting
    body: where your data flows
    href: /docs
    linkLabel: Docs
    icon: ri-key-2-line
  - title: Roles and permissions
    body: who can build, connect, and change what
    href: /docs
    linkLabel: Docs
    icon: ri-lock-2-line
  - title: Audit logs and data handling
    body: what's recorded and how it's stored
    href: /docs
    linkLabel: Docs
    icon: ri-file-list-3-line

finalCta:
  headline: Put agents to work, safely.
---

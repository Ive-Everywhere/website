---
# Transcribed verbatim from
# `content-src/eluu-website-content/finalized-pages/agents.md` (DRAFT v4).
# The `[Advantage]` / `[Speed]` / `[Cost]` / `[Risk]` tags and the italic
# `*(...)*` notes are internal value drivers, not page copy, so they are not
# carried here. `[VERIFY]` becomes `needsVerify: true`.
title: Agents
description: Each agent comes built for its job and connected to your tools. Hand it work in Slack, and it runs in the cloud while you get on with your day.
publishedAt: 2026-08-03
order: 1

hero:
  eyebrow: Agents
  headline: An AI specialist for every role on your team.
  subhead: Each one comes built for its job and connected to your tools. Hand it work in Slack, and it runs in the cloud while you get on with your day.

pitch:
  heading: Built for the role, ready to work.
  body: Each agent is built for a specific role, with the tools, skills, and knowledge that role needs wired in and tested on real work. Most AI is an experiment. An agent isn't. And it runs in the cloud, so you hand off the work and close your laptop.
  linkLabel: See the agent library
  linkHref: /cookbook
  # Source visual note: "product clip (an agent running a real task end to end)".
  art: c1-a-compose

stats:
  - value: Minutes
    label: to put a prebuilt specialist to work
    needsVerify: true
  - value: 24/7
    label: running in the cloud, working while you sleep
  - value: 3 surfaces
    label: 'it shows up on: Slack, web, and email'

features:
  - headline: Built and tested for the job
    eyebrow: Not a prompt you hope works.
    body: Each agent is assembled for its role and run against real work before it touches yours. It does the job, it isn't a demo of the job.
    icon: ri-verified-badge-line
  - headline: Reach it where you work
    eyebrow: Hand it off and close your laptop.
    body: Give it a task in Slack, on the web, or over email. It works in the cloud, and the finished work comes back to you in the same place you asked.
    icon: ri-slack-line
  - headline: It learns your business
    eyebrow: The longer it works, the more it knows.
    body: Every task teaches it your accounts, your process, your exceptions. It doesn't reset between sessions, and that knowledge doesn't leave when someone quits.
    icon: ri-brain-line
  - headline: Prebuilt, or build your own
    eyebrow: Start in minutes, or build for any job.
    body: Pick a specialist for a common role, or build one yourself. Name the role, connect the tools, set how it works. No code.
    icon: ri-robot-3-line
  # The source ends this card with `→ /security`, which is a real link.
  - headline: You stay in control
    eyebrow: You see what every agent can touch.
    body: What data it can reach, what it can do, and every step it took. Nothing runs where you can't see it.
    icon: ri-eye-line
    linkHref: /security
    linkLabel: Security

# The source holds no testimonial: only `[VERIFY: one anonymized line,
# role-attributed.]`. Nothing is invented, so the placeholder itself is
# transcribed and rendered inside `.copy-needed`.
testimonial:
  quote: 'VERIFY: one anonymized line, role-attributed.'
  attribution: role-attributed, anonymized
  needsVerify: true

technical:
  - title: How an agent is configured
    body: identity, skills, tools, behaviour, integrations
    href: /docs
    linkLabel: Docs
    icon: ri-tools-line
  - title: How agents run in the cloud
    body: the runtime, scheduling, hand-off
    href: /docs
    linkLabel: Docs
    icon: ri-cloud-line
  - title: How agent memory works
    body: what it learns, where it's stored, who sees it
    href: /docs
    linkLabel: Docs
    icon: ri-brain-line

finalCta:
  headline: Start with one specialist. Grow a team.
---

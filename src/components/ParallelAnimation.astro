---
// Parallel execution animation — 4 colleagues processing tasks simultaneously
---

<div class="pa-wrap">
  <img src="/images/cookbook-bg.png" alt="" class="pa-bg" />

  <div class="pa-window">
    <div class="pa-chrome">
      <div class="pa-dots">
        <span style="background:#e8655a;"></span>
        <span style="background:#e8c15a;"></span>
        <span style="background:#67c96c;"></span>
      </div>
      <div class="pa-chrome-right">
        <span>Colleagues</span>
      </div>
    </div>

    <div class="pa-body" id="pa-body">
      <!-- Row 1: Lewis -->
      <div class="pa-row pa-r1">
        <img src="/images/avatar-lewis.png" alt="" class="pa-avatar" />
        <div class="pa-info">
          <div class="pa-top">
            <span class="pa-name">Lewis</span>
            <span class="pa-role">Marketing</span>
          </div>
          <div class="pa-task">Enriching 47 contacts from HubSpot</div>
          <div class="pa-bar-row">
            <div class="pa-bar"><div class="pa-fill pa-f1"></div></div>
            <div class="pa-icons">
              <img src="/logos/integrations/hubspot.svg" alt="" />
              <img src="/logos/integrations/salesforce.svg" alt="" />
            </div>
            <div class="pa-done pa-d1">&#10003;</div>
          </div>
        </div>
      </div>

      <!-- Row 2: Iris -->
      <div class="pa-row pa-r2">
        <img src="/images/avatar-iris.png" alt="" class="pa-avatar" />
        <div class="pa-info">
          <div class="pa-top">
            <span class="pa-name">Iris</span>
            <span class="pa-role">Product</span>
          </div>
          <div class="pa-task">Analyzing feature requests from feedback</div>
          <div class="pa-bar-row">
            <div class="pa-bar"><div class="pa-fill pa-f2"></div></div>
            <div class="pa-icons">
              <img src="/logos/integrations/notion.svg" alt="" style="filter:invert(0.5);" />
              <img src="/logos/integrations/intercom.svg" alt="" />
            </div>
            <div class="pa-done pa-d2">&#10003;</div>
          </div>
        </div>
      </div>

      <!-- Row 3: Priya -->
      <div class="pa-row pa-r3">
        <img src="/images/avatar-priya.png" alt="" class="pa-avatar" />
        <div class="pa-info">
          <div class="pa-top">
            <span class="pa-name">Priya</span>
            <span class="pa-role">Sales</span>
          </div>
          <div class="pa-task">Generating Q3 pipeline forecast</div>
          <div class="pa-bar-row">
            <div class="pa-bar"><div class="pa-fill pa-f3"></div></div>
            <div class="pa-icons">
              <img src="/logos/integrations/googlesheets.svg" alt="" />
              <img src="/logos/integrations/slack.svg" alt="" />
            </div>
            <div class="pa-done pa-d3">&#10003;</div>
          </div>
        </div>
      </div>

      <!-- Row 4: James -->
      <div class="pa-row pa-r4">
        <img src="/images/avatar-james.png" alt="" class="pa-avatar" />
        <div class="pa-info">
          <div class="pa-top">
            <span class="pa-name">James</span>
            <span class="pa-role">Engineering</span>
          </div>
          <div class="pa-task">Running PR review on sprint backlog</div>
          <div class="pa-bar-row">
            <div class="pa-bar"><div class="pa-fill pa-f4"></div></div>
            <div class="pa-icons">
              <img src="/logos/integrations/github.svg" alt="" style="filter:invert(0.5);" />
              <img src="/logos/integrations/jira.svg" alt="" />
            </div>
            <div class="pa-done pa-d4">&#10003;</div>
          </div>
        </div>
      </div>

      <!-- Row 5: Bonus — queued after first batch -->
      <div class="pa-row pa-r5" id="pa-bonus">
        <img src="/images/avatar-iris.png" alt="" class="pa-avatar" />
        <div class="pa-info">
          <div class="pa-top">
            <span class="pa-name">Iris</span>
            <span class="pa-role">Product</span>
          </div>
          <div class="pa-task">Drafting weekly changelog from merged PRs</div>
          <div class="pa-bar-row">
            <div class="pa-bar"><div class="pa-fill pa-f5"></div></div>
            <div class="pa-icons">
              <img src="/logos/integrations/github.svg" alt="" style="filter:invert(0.5);" />
              <img src="/logos/integrations/notion.svg" alt="" style="filter:invert(0.5);" />
            </div>
            <div class="pa-done pa-d5">&#10003;</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .pa-wrap {
    position: absolute;
    inset: 0;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 24px;
    box-sizing: border-box;
  }

  .pa-bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 0;
  }

  .pa-window {
    position: relative;
    z-index: 1;
    width: 100%;
    max-width: 460px;
    background: rgba(250, 249, 244, 0.95);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-radius: 12px;
    box-shadow: 0 20px 60px rgba(0,0,0,0.25), 0 0 0 1px rgba(255,255,255,0.1);
    overflow: hidden;
    font-family: 'Inter', sans-serif;
  }

  .pa-chrome {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 10px 14px;
    border-bottom: 1px solid rgba(0,0,0,0.06);
  }
  .pa-dots { display: flex; gap: 6px; }
  .pa-dots span { width: 10px; height: 10px; border-radius: 50%; }
  .pa-chrome-right { font-size: 13px; color: #17100e; font-weight: 600; }

  .pa-body { padding: 4px 0; }

  .pa-row {
    display: flex;
    gap: 10px;
    padding: 8px 14px;
    border-bottom: 1px solid rgba(0,0,0,0.04);
    opacity: 0;
    transform: translateY(8px);
  }
  .pa-row:last-child { border-bottom: none; }

  .pa-avatar {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    flex-shrink: 0;
    object-fit: cover;
  }

  .pa-info { flex: 1; min-width: 0; }

  .pa-top {
    display: flex;
    align-items: baseline;
    gap: 5px;
    margin-bottom: 2px;
  }
  .pa-name { font-size: 12px; font-weight: 600; color: #17100e; }
  .pa-role { font-size: 10px; color: #a3a3a3; }

  .pa-task {
    font-size: 10px;
    color: #7d7d7d;
    margin-bottom: 6px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .pa-bar-row {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .pa-bar {
    flex: 1;
    height: 3px;
    background: rgba(0,0,0,0.06);
    border-radius: 2px;
    overflow: hidden;
  }
  .pa-fill {
    height: 100%;
    background: #17100e;
    border-radius: 2px;
    width: 0%;
  }

  .pa-icons {
    display: flex;
    gap: 4px;
    flex-shrink: 0;
  }
  .pa-icons img {
    width: 14px;
    height: 14px;
    opacity: 0.45;
  }

  .pa-done {
    font-size: 12px;
    color: #17100e;
    opacity: 0;
    transform: scale(0);
    flex-shrink: 0;
    width: 16px;
    text-align: center;
  }
</style>

<script>
(function() {
  const body = document.getElementById('pa-body');
  if (!body) return;

  const mainRows = Array.from(body.querySelectorAll('.pa-row')).slice(0, 4) as HTMLElement[];
  const bonusRow = document.getElementById('pa-bonus') as HTMLElement;
  const allRows = [...mainRows, bonusRow];
  const mainFills = Array.from(body.querySelectorAll('.pa-fill')).slice(0, 4) as HTMLElement[];
  const bonusFill = body.querySelector('.pa-f5') as HTMLElement;
  const mainDones = Array.from(body.querySelectorAll('.pa-done')).slice(0, 4) as HTMLElement[];
  const bonusDone = body.querySelector('.pa-d5') as HTMLElement;

  const durations = [3200, 3800, 3500, 4100];
  const BONUS_DUR = 2800;
  const STAGGER_IN = 100;
  const FADE_IN = 400;
  const FADE_OUT = 500;

  function reset() {
    allRows.forEach(r => {
      r.style.transition = 'none';
      r.style.opacity = '0';
      r.style.transform = 'translateY(8px)';
    });
    [...mainFills, bonusFill].forEach(f => {
      f.style.transition = 'none';
      f.style.width = '0%';
    });
    [...mainDones, bonusDone].forEach(d => {
      d.style.transition = 'none';
      d.style.opacity = '0';
      d.style.transform = 'scale(0)';
    });
    void body.offsetHeight;
  }

  function sleep(ms: number) {
    return new Promise(r => setTimeout(r, ms));
  }

  async function run() {
    while (true) {
      reset();
      await sleep(300);

      // Stagger first 4 rows in
      for (let i = 0; i < mainRows.length; i++) {
        setTimeout(() => {
          mainRows[i].style.transition = `all ${FADE_IN}ms cubic-bezier(0.16, 1, 0.3, 1)`;
          mainRows[i].style.opacity = '1';
          mainRows[i].style.transform = 'translateY(0)';
        }, i * STAGGER_IN);
      }
      await sleep(FADE_IN + mainRows.length * STAGGER_IN);

      // Start all 4 progress bars simultaneously
      mainFills.forEach((f, i) => {
        f.style.transition = `width ${durations[i]}ms cubic-bezier(0.4, 0, 0.2, 1)`;
        f.style.width = '100%';
      });

      // Show checkmarks as each finishes
      const sorted = durations.map((d, idx) => ({ d, idx })).sort((a, b) => a.d - b.d);
      for (let i = 0; i < sorted.length; i++) {
        setTimeout(() => {
          const idx = sorted[i].idx;
          mainDones[idx].style.transition = 'all 300ms cubic-bezier(0.16, 1, 0.3, 1)';
          mainDones[idx].style.opacity = '1';
          mainDones[idx].style.transform = 'scale(1)';
        }, sorted[i].d);
      }

      // Wait for all 4 to finish
      await sleep(Math.max(...durations) + 400);

      // 5th task slides in
      bonusRow.style.transition = `all ${FADE_IN}ms cubic-bezier(0.16, 1, 0.3, 1)`;
      bonusRow.style.opacity = '1';
      bonusRow.style.transform = 'translateY(0)';
      await sleep(500);

      // Its bar starts filling
      bonusFill.style.transition = `width ${BONUS_DUR}ms cubic-bezier(0.4, 0, 0.2, 1)`;
      bonusFill.style.width = '100%';
      await sleep(BONUS_DUR);

      // Checkmark
      bonusDone.style.transition = 'all 300ms cubic-bezier(0.16, 1, 0.3, 1)';
      bonusDone.style.opacity = '1';
      bonusDone.style.transform = 'scale(1)';
      await sleep(1200);

      // Fade out everything
      allRows.forEach(r => {
        r.style.transition = `all ${FADE_OUT}ms cubic-bezier(0.4, 0, 0.2, 1)`;
        r.style.opacity = '0';
        r.style.transform = 'translateY(-6px)';
      });
      await sleep(FADE_OUT + 400);
    }
  }

  // Start when visible
  const io = new IntersectionObserver((entries) => {
    if (entries[0].isIntersecting) {
      io.disconnect();
      run();
    }
  }, { threshold: 0.3 });
  io.observe(body);
})();
</script>

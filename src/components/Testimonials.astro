---
const testimonials = [
  {
    quote: 'Eluu colleagues run daily briefings, track pipelines, and log in our deals — all from Slack. What used to take 3 hours every morning is just done.',
    name: 'Sarah Chen',
    role: 'Head of Revenue',
    company: 'TestMu AI',
  },
  {
    quote: "With Eluu colleagues, our team completes projects which took a week to completion in a couple of hours. We couldn't go back to the old way.",
    name: 'Raj Patel',
    role: 'Co-founder',
    company: 'Loom',
  },
  {
    quote: 'We connected 4 tools and had an engineering colleague running PR reviews in our sprint health. Setup took less than a day and we were seeing results immediately.',
    name: 'James Liu',
    role: 'Engineering Lead',
    company: 'RIPPLING',
  },
];
---

<section style="background: #f4f3ec; padding: 80px 24px;">
  <div style="max-width: 1440px; margin: 0 auto; padding: 0 0;">
    <!-- Company tabs -->
    <div class="ts-tabs reveal">
      <button type="button" class="ts-tab active" data-ts="0">
        <img src="/logos/testimonials/testmu.svg" alt="TestMu AI" class="ts-logo ts-logo-testmu" />
      </button>
      <button type="button" class="ts-tab" data-ts="1">
        <img src="/logos/testimonials/loom.svg" alt="Loom" class="ts-logo ts-logo-loom" />
      </button>
      <button type="button" class="ts-tab" data-ts="2">
        <img src="/logos/testimonials/rippling.svg" alt="Rippling" class="ts-logo ts-logo-rippling" />
      </button>
    </div>

    <!-- Quotes container -->
    <div class="ts-quotes">
      {testimonials.map((t, i) => (
        <div class={`ts-quote ${i === 0 ? 'active' : ''}`} data-tq={i}>
          <blockquote>
            &ldquo;{t.quote}&rdquo;
          </blockquote>
          <div class="ts-attr">
            <p>
              <span class="ts-name">{t.name}</span>, {t.role}
            </p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  /* ── Tabs ── */
  .ts-tabs {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    gap: 36px;
    margin-bottom: 40px;
  }
  @media (min-width: 640px) { .ts-tabs { gap: 56px; margin-bottom: 56px; } }
  @media (min-width: 1024px) { .ts-tabs { gap: 72px; } }

  .ts-tab {
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
    transition: opacity 0.3s ease;
    opacity: 0.3;
  }
  .ts-tab:hover { opacity: 0.55; }
  .ts-tab.active { opacity: 1; }

  .ts-logo {
    display: block;
    filter: brightness(0);
    object-fit: contain;
  }

  /* Each logo gets its own width so the TEXT portion aligns visually */
  .ts-logo-testmu { width: 140px; }
  .ts-logo-loom { width: 120px; }
  .ts-logo-rippling { width: 180px; }

  @media (min-width: 640px) {
    .ts-logo-testmu { width: 170px; }
    .ts-logo-loom { width: 150px; }
    .ts-logo-rippling { width: 220px; }
  }
  @media (min-width: 1024px) {
    .ts-logo-testmu { width: 200px; }
    .ts-logo-loom { width: 180px; }
    .ts-logo-rippling { width: 260px; }
  }

  /* ── Quotes container ── */
  .ts-quotes {
    position: relative;
    min-height: 220px;
    text-align: center;
    max-width: 1060px;
    margin: 0 auto;
  }
  @media (min-width: 640px) { .ts-quotes { min-height: 320px; } }
  @media (min-width: 768px) { .ts-quotes { min-height: 420px; } }
  @media (min-width: 1024px) { .ts-quotes { min-height: 460px; } }

  /* ── Single quote ── */
  .ts-quote {
    position: absolute;
    inset: 0;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.6s ease;
  }
  .ts-quote.active {
    opacity: 1;
    pointer-events: auto;
  }

  blockquote {
    font-family: 'Season Mix', sans-serif;
    font-weight: 400;
    font-size: 22px;
    line-height: 1.25;
    letter-spacing: -0.3px;
    color: #17100e;
    margin: 0;
    padding-bottom: 32px;
    text-indent: -0.4em;
  }
  @media (min-width: 640px) {
    blockquote { font-size: 36px; letter-spacing: -0.5px; padding-bottom: 48px; }
  }
  @media (min-width: 768px) {
    blockquote { font-size: 52px; letter-spacing: -0.8px; }
  }
  @media (min-width: 1024px) {
    blockquote { font-size: 68px; letter-spacing: -1.5px; }
  }

  /* ── Attribution ── */
  .ts-attr {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
  }

  .ts-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: #4ade80;
    flex-shrink: 0;
  }

  .ts-attr p {
    font-family: 'Inter', sans-serif;
    font-size: 16px;
    color: #17100e;
    margin: 0;
  }

  .ts-name {
    font-weight: 600;
  }
</style>

<script>
  (function () {
    const tabs = document.querySelectorAll('.ts-tab');
    const quotes = document.querySelectorAll('.ts-quote');
    if (!tabs.length) return;

    function show(idx) {
      tabs.forEach((t, i) => t.classList.toggle('active', i === idx));
      quotes.forEach((q, i) => q.classList.toggle('active', i === idx));
    }

    tabs.forEach((tab) => {
      tab.addEventListener('click', () => show(parseInt(tab.dataset.ts)));
    });

    // Auto-rotate every 6 seconds
    let current = 0;
    let timer = setInterval(() => {
      current = (current + 1) % tabs.length;
      show(current);
    }, 6000);

    // Reset timer on manual click
    tabs.forEach((tab) => {
      tab.addEventListener('click', () => {
        current = parseInt(tab.dataset.ts);
        clearInterval(timer);
        timer = setInterval(() => {
          current = (current + 1) % tabs.length;
          show(current);
        }, 6000);
      });
    });
  })();
</script>

---
// StatsBand — 3 stat cards per ona ref (case frame-01: "10x / 0% / 24/7"):
// three rounded panels in a row, HUGE display numeral top-left, small muted
// label under it, generous vertical padding.
// Missing values ([VERIFY] slots) render as em-dashes — never invented numbers.
interface Stat {
  value?: string;
  label: string;
}
interface Props {
  stats: Stat[];
}
const { stats } = Astro.props;
---

<section class="osb">
  <div class="osb-container">
    <div class="osb-grid reveal">
      {stats.map((stat) => (
        <div class="osb-item">
          <span class="osb-value">{stat.value ?? '—'}</span>
          <span class="osb-label">{stat.label}</span>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .osb {
    background: #13100a;
    padding: 96px 0 0;
  }
  .osb-container {
    max-width: 1380px;
    margin: 0 auto;
    padding: 0 20px;
    box-sizing: border-box;
  }
  .osb-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 16px;
  }
  @media (min-width: 640px) {
    .osb-grid { grid-template-columns: repeat(3, 1fr); }
  }

  .osb-item {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 12px;
    background: #1e1d1b;
    border-radius: 2px;
    padding: 40px 32px 44px;
    box-sizing: border-box;
    overflow: hidden;
  }
  .osb-item::before {
    content: "";
    pointer-events: none;
    position: absolute;
    inset: 0;
    border-radius: 2px;
    border: 1px solid rgba(237, 233, 227, 0.06);
    z-index: 30;
  }
  .osb-value {
    font-family: 'Söhne', sans-serif;
    font-size: clamp(36px, 4vw, 56px);
    font-weight: 600;
    color: #ede9e3;
    line-height: 1;
    letter-spacing: -0.025em;
    font-variant-numeric: tabular-nums;
  }
  .osb-label {
    font-family: 'Söhne', sans-serif;
    font-size: 0.9375rem;
    font-weight: 400;
    color: rgba(237, 233, 227, 0.5);
    line-height: 1.45;
    text-wrap: pretty;
  }

  @media (max-width: 639px) {
    .osb { padding: 64px 0 0; }
    .osb-item { padding: 28px 24px 32px; }
  }
</style>
